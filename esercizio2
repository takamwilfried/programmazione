//Print all the numbers divisible by 5 and 7, from 1 to 100.

for (int i=1; i<=100; i++)
    {
        var r = i % 5;
        var t =i % 7;
        if(r==0)
        if(t==0)
        Console.WriteLine(i);

    }
Console.ReadKey();cd 
